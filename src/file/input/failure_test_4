PROGRAM prueba;

REAL :: velocity = 3.0;
REAL :: distance;

velocity = velocity + 5.0;
CALL calculate_pos(velocity, time);

END PROGRAM prueba

SUBROUTINE calculate_pos(v, t)
    REAL, INTENT(IN) v;
    REAL :: p;
    p = v * t + 0.0;
END SUBROUTINE calculate_pos