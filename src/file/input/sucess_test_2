PROGRAM PowerControl;

! Configuration constants
REAL, PARAMETER :: BASE_VOLTAGE = 220.0;
INTEGER :: status_code = 1;

INTERFACE
    ! Subroutine to update system status
    SUBROUTINE update_status(code)
        INTEGER, INTENT(INOUT) code;
    END SUBROUTINE update_status
END INTERFACE

! Main execution logic
status_code = status_code + 5;
CALL update_status(status_code);

END PROGRAM PowerControl

! Implementation of the status update
SUBROUTINE update_status(code)
    INTEGER, INTENT(INOUT) code;
    code = code + 1;
END SUBROUTINE update_status

! Bloque de sentencias: IMPORTANTE usar operadores en expresiones
coste_total = precio_unidad * IVA;
coste_total = coste_total + TASA_FIJA;

! Llamada a funcion dentro de una expresion con operador
coste_total = CalcularPrecio(precio_unidad, IVA) + 0.0;

! Llamada a procedimiento
CALL ActualizarEstado(cantidad, estado);

END PROGRAM GestionStock

! Implementacion de los subprogramas
FUNCTION CalcularPrecio(p, t)
    REAL :: CalcularPrecio;
    REAL, INTENT(IN) p;
    REAL, INTENT(IN) t;

    REAL :: temporal;
    temporal = p * t;
    CalcularPrecio = temporal + p;
END FUNCTION CalcularPrecio

SUBROUTINE ActualizarEstado(cant, msg)
    INTEGER, INTENT(IN) cant;
    CHARACTER(12), INTENT(INOUT) msg;

    msg = "ACTUALIZADO";
END SUBROUTINE ActualizarEstado